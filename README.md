# Prime Fitness

A JavaFX fitness-management application backed by MySQL.

The application entry point is `Sda Project/src/application/Main.java`; database access lives under `Sda Project/src/db/`.

The module requires JavaFX Base, Graphics, FXML, Controls, and `java.sql`.
